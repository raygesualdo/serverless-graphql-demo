service: serverless-graphql

provider:
  name: aws
  runtime: nodejs8.10
  profile: personal

functions:
  graphql:
    handler: handler.express
    events:
      - http:
          path: /
          method: post
  playground:
    handler: handler.express
    events:
      - http:
          path: /
          method: get

plugins:
  - serverless-dotenv-plugin
  - serverless-offline

custom:
  dotenv:
    path: ../.env
